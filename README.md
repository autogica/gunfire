# gunfire

## Install

    $ npm install gunfire -g

## Usage

Go to your fork of Autogica's git repository (atgc.git),
then run:

    $ gunfire

## Developers

    $ npm install
    $ bower install
    $ gulp
    $ npm link
